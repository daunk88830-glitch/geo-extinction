# 내일 할 일

마지막 작업: 2026-08-01

앱은 다 만들어졌고 로컬에서 잘 돕니다. 남은 건 **배포 설정 3가지**입니다.
전부 웹 화면에서 하는 일이고, 다 하면 30~40분쯤 걸립니다.

---

## 순서를 지켜 주세요

`VITE_` 로 시작하는 값은 **빌드할 때 코드 안에 박힙니다.** 나중에 Netlify에서
바꿔도 이미 만들어진 사이트는 안 바뀝니다.

> **환경변수 먼저 → 그다음 푸시**

순서가 바뀌면 재배포를 한 번 더 해야 합니다.

---

## 1. Netlify 환경변수 — 대부분 끝났습니다

2026-08-01 배포에서 확인한 결과:

| Key | 현재 값 | 상태 |
|---|---|---|
| `VITE_FB_*` 6개 | (등록됨) | ✅ |
| `VITE_SESSION_PREFIX` | `2026-2학기` | ✅ 코드가 하이픈을 자동으로 붙입니다 |
| `VITE_SESSION_ID` | `2026-07-30-1` | ⚠️ `default` 로 바꾸면 좋음 (급하지 않음) |
| `ANTHROPIC_API_KEY` | ? | 없으면 반론이 준비된 문장으로 나옴 |

`VITE_SESSION_ID` 는 **학생이 반을 못 고른 예외 상황에만 쓰이는 예비값**이라
그대로 둬도 수업은 정상입니다. 바꾸려면
**Project configuration → Environment variables → 해당 줄의 ⋯ → Edit**.

### 같이 확인할 것

`ANTHROPIC_API_KEY` 가 등록되어 있는지 보세요.

- 이름 앞에 **`VITE_` 가 붙으면 안 됩니다.** 붙이면 API 키가 학생 브라우저로
  그대로 전송됩니다.
- 없어도 수업은 됩니다 — 반론이 AI 대신 미리 준비된 문장으로 나옵니다.

---

## 2. Firebase 승인된 도메인 추가 ← 이걸 빼면 아무도 못 들어옵니다

Firebase 콘솔 → **Authentication → Settings(설정) → 승인된 도메인 → 도메인 추가**

```
geo-extinction.netlify.app
```

지금은 `localhost` 만 등록되어 있습니다. 이 상태로 수업에 들어가면
**학생 30명 전원이 로그인 단계에서 막힙니다.** 화면에는 "로그인에 실패했습니다"
만 뜨고 원인을 알 방법이 없습니다. 교실에서 가장 위험한 항목입니다.

---

## 3. 푸시 — 끝났습니다

2026-08-01 에 밀었고 Netlify 배포도 **Published** 로 끝났습니다.
주소는 **https://geo-extinction.netlify.app** 입니다.

---

## 그다음 (수업 전에 한 번은)

- [ ] 배포된 주소로 **직접 들어가서 처음부터 끝까지 해 보기**
      (홈 → 선개념 → 타임라인 → 데이터 탐구 → 원인 판정 → 형성평가)
      AI 반론 3개는 아직 로컬에서 대체 문장으로만 돌아 봤습니다.
      실제로 AI가 응답하는지는 배포 후에만 확인됩니다.
- [ ] 폰으로도 한 번 열어 보기 (학생은 전부 스마트폰입니다)
- [ ] 교사 대시보드를 쓰려면 Firebase 콘솔에서 최상위 `teachers/{내 uid}`
      문서를 만들어야 합니다. 학생 수업에는 영향이 없으니 나중에 해도 됩니다.
- [ ] 형성평가 문항을 구글 시트로 관리하려면 `VITE_QUIZ_SHEET_ID` 등록
      (비워 두면 `public/data/quiz-sample.json` 을 씁니다)

---

## 오늘 끝낸 것 (참고)

- 원인 판정: 「미검토」에서 안 바뀌던 저장 문제 해결 — 보안 규칙이 부분 저장을
  거부하고 있었고, 실패가 화면에 안 뜨고 있었습니다. 둘 다 고쳤습니다.
- 모둠을 5개로 (대멸종 5개 = 모둠 5개). 모둠 수는 `src/data/groups.js`
  한 곳에서 바꿉니다.
- 원인 판정 화면 안에 **「자료 다시 보기」** 추가 — 판정·재반박을 쓰면서
  다양성·화석 수·기온·CO₂ 그래프를 그 자리에서 다시 볼 수 있습니다.

---

## 개발 서버 다시 켜기

```
npm run dev
```

주소는 http://localhost:5173 입니다.
